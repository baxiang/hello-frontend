# React 路由管理 ⭐⭐

> React Router v6 路由配置、嵌套路由、编程式导航

---

## L1 理解层

**最简示例（1-3行）：**

```tsx
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom'

function App() {
  return (
    <BrowserRouter>
      <nav><Link to="/">首页</Link><Link to="/about">关于</Link></nav>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
      </Routes>
    </BrowserRouter>
  )
}
```

**详细示例：**

```tsx
import { BrowserRouter, Routes, Route, Link, Outlet, useParams, useNavigate } from 'react-router-dom'

// 布局组件
function Layout() {
  return (
    <div>
      <nav>
        <Link to="/">首页</Link>
        <Link to="/posts">文章</Link>
      </nav>
      <Outlet /> {/* 子路由渲染位置 */}
    </div>
  )
}

// 动态路由
function PostDetail() {
  const { id } = useParams()
  return <h1>文章 {id}</h1>
}

// 嵌套路由
function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Layout />}>
          <Route index element={<Home />} />
          <Route path="posts" element={<PostList />} />
          <Route path="posts/:id" element={<PostDetail />} />
          <Route path="*" element={<NotFound />} />
        </Route>
      </Routes>
    </BrowserRouter>
  )
}
```

---

## L2 实践层

### 导航方法

| 方法 | 用途 | 示例 |
|------|------|------|
| `<Link>` | 声明式导航 | `<Link to="/about">` |
| `useNavigate` | 编程式导航 | `navigate('/about')` |
| `useParams` | 获取路由参数 | `const { id } = useParams()` |
| `useSearchParams` | 获取查询参数 | `const [search] = useSearchParams()` |

```tsx
'use client'
import { useNavigate, useSearchParams } from 'react-router-dom'

function SearchPage() {
  const navigate = useNavigate()
  const [searchParams] = useSearchParams()
  const q = searchParams.get('q')

  return (
    <div>
      <p>搜索：{q}</p>
      <button onClick={() => navigate('/results', { state: { query: q } })}>
        搜索
      </button>
    </div>
  )
}
```

### 路由守卫

```tsx
function ProtectedRoute({ children }: { children: React.ReactNode }) {
  const isAuthenticated = useAuth()
  const navigate = useNavigate()

  if (!isAuthenticated) {
    navigate('/login', { replace: true })
    return null
  }

  return <>{children}</>
}

// 使用
<Route path="/dashboard" element={<ProtectedRoute><Dashboard /></ProtectedRoute>} />
```

---

## 学习资源

- [React Router 官方文档](https://reactrouter.com/) ⭐ 官方权威
