# 框架集成指南

> React、Vue、Node.js 中的 TypeScript

---

## L1 理解层

**React 组件类型：**

```typescript
interface ButtonProps { text: string; onClick: () => void; disabled?: boolean }
function Button({ text, onClick, disabled }: ButtonProps) {
    return <button onClick={onClick} disabled={disabled}>{text}</button>;
}
```

**Vue 组件类型：**

```vue
<script setup lang="ts">
interface Props { title: string; count?: number }
const props = withDefaults(defineProps<Props>(), { count: 0 });
const emit = defineEmits<{ (e: 'update', value: number): void }>();
</script>
```

**Express 类型：**

```typescript
import { Request, Response, NextFunction } from 'express';
app.get('/users', (req: Request, res: Response) => { res.json({ users: [] }); });
```

---

## L2 实践层

### React Hooks 类型

```typescript
const [count, setCount] = useState(0);
const ref = useRef<HTMLDivElement>(null);
const callback = useCallback(() => { }, []);
const memoized = useMemo(() => expensive(), [dep]);
```

### Vue Props 类型

```vue
<script setup lang="ts">
interface Props { id: number; name: string; tags?: string[] }
const props = defineProps<Props>();
</script>
```

---

## 学习资源

- [React + TypeScript Cheat Sheet](https://react-typescript-cheatsheet.netlify.app/)
- [Vue TypeScript 指南](https://cn.vuejs.org/guide/typescript/overview.html) ⭐ 官方权威
